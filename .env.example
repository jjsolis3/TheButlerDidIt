# Copy to .env for local docker compose runs. In Coolify, set these in the
# resource's "Environment Variables" tab instead.
POSTGRES_PASSWORD=change-me-to-something-long
ALLOW_REGISTRATION=true
APP_PORT=8080
